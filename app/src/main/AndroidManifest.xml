<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Xposed 模块声明 -->
        <meta-data
            android:name="xposed_module"
            android:value="true" />
        <meta-data
            android:name="xposed_minversion"
            android:value="93" />
        <meta-data
            android:name="xposed_scope"
            android:value="com.tencent.mm" />
        <meta-data
            android:name="xposed_description"
            android:value="隐藏微信好友 - 不删除不屏蔽,好友仍可发消息打电话" />

        <meta-data
            android:name="xposed_init"
            android:value="wx.mirage.MainHook" />

    </application>
</manifest>
